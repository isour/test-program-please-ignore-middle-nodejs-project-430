.git
node_modules
tmp

# Собранную статику кладёт `make build` уже внутри образа. Без этих строк рядом с ней окажутся
# файлы от локальной сборки — с другими хешами в именах, то есть мусор.
public/assets
public/index.html
